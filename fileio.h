#include "hex.h"

void savefile(cellData* data, int id, int* dims, int val, int iter); //sauvegarde de data au format data_mpi[n].txt
void readfile(cellData* data, int id, int* dims); //sauvegarde de data au format data_mpi[n].txt
